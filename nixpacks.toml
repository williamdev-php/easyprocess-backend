[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "playwright-driver.browsers"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt", "playwright install chromium"]

[variables]
PLAYWRIGHT_BROWSERS_PATH = "/app/.playwright"
